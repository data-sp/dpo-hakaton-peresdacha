# ChemAI: Predict the Cure
# ДПО Команда Хакатон-Пересдача

Решение задачи **ChemAI: Predict the Cure**.

Цель соревнования — построить ML-пайплайн, который по числовым молекулярным дескрипторам химического соединения предсказывает три показателя биологической активности против вируса гриппа:

* `IC50` — концентрация, при которой вещество подавляет 50% активности вируса;
* `CC50` — концентрация, при которой вещество токсично для 50% клеток;
* `SI` — индекс селективности.

Качество решения оценивается на Kaggle по метрике RMSE, усредненной по трем таргетам.

**Финальный public Kaggle score:** `279.50 RMSE`

---

## Структура репозитория

```text
.
├── ChemAI_final.ipynb        # основной ноутбук с EDA, моделированием и генерацией submission
├── README.md                 # описание подхода и инструкция по воспроизведению
├── presentation.pdf          # презентация проекта
├── leaderboard.png           # скриншот лидерборда
```

Данные не включены в репозиторий, так как по условию соревнования они являются закрытыми данными лаборатории и предоставляются отдельно организаторами.

---

## Используемые данные

В решении используются файлы, предоставленные организаторами:

- `train.csv` — обучающая выборка с молекулярными дескрипторами и известными значениями `IC50`, `CC50`, `SI`;
- `test.csv` — тестовая выборка с молекулярными дескрипторами;
- `sample_submission.csv` — шаблон файла для отправки на Kaggle.

Данные необходимо скачать по ссылке, указанной в условии соревнования, и разместить рядом с ноутбуком локально:

```text
train.csv
test.csv
sample_submission.csv
```

Внешние данные не используются. Значения таргетов из тестовой выборки не используются.

---

## Краткое описание подхода

Решение построено как воспроизводимый ML-пайплайн

Основные этапы:

1. Загрузка данных и проверка структуры `train` и `test`.
2. Проверка совпадения признаков в обучающей и тестовой выборках.
3. Анализ пропусков.
4. Анализ распределений целевых переменных.
5. Проверка выбросов и длинных правых хвостов у `IC50`, `CC50` и `SI`.
6. Проверка связи между `SI`, `IC50` и `CC50`.
7. Тестирование дополнительного feature engineering.
8. Сравнение базовых моделей.
9. Подбор финальной конфигурации по кросс-валидации.
10. Обучение seed-ансамбля.
11. Добавление независимой QSAR-SVR модели.
12. Усреднение двух подходов и генерация итогового `submission.csv`.

---

## Анализ таргетов

Целевые переменные имеют выраженную асимметрию и выбросы. Это важно, потому что RMSE чувствителен к большим ошибкам на крупных значениях.

Поэтому в решении отдельно анализируются:

* исходные распределения таргетов;
* распределения после преобразования `log1p`;
* влияние выбросов на качество кросс-валидации;
* устойчивость моделей на центральной части train-распределения.

Для части моделей используется преобразование таргета:

```python
y_transformed = np.log1p(y)
```

Обратное преобразование выполняется так:

```python
y_pred = np.expm1(y_pred_transformed)
```

Такой подход помогает сделать обучение более устойчивым на данных с длинными правыми хвостами.

---

## Работа с `SI`

В обучающей выборке `SI` практически совпадает с отношением:

```text
SI = CC50 / IC50
```

Однако по условию соревнования `SI` необходимо предсказывать как отдельную переменную. Поэтому эта связь используется только для анализа данных и интерпретации.

В финальном пайплайне:

* `SI` обучается отдельной моделью;
* признаки для модели `SI` — только молекулярные дескрипторы;
* нет шага `SI = CC50 / IC50`;
* `SI` не вычисляется из предсказанных `IC50` и `CC50`.

Такой подход позволяет не переносить ошибки предсказания IC50 и CC50 в третий таргет и сохраняет единый формат решения: все три показателя предсказываются моделями машинного обучения.

---

## Feature engineering

Была проверена гипотеза о добавлении дополнительных групповых признаков на основе исходных молекулярных дескрипторов:

* диапазоны зарядов;
* диапазоны EState;
* отношения молекулярных масс и атомных характеристик;
* агрегаты по VSA-признакам;
* агрегаты по функциональным группам `fr_*`.

После проверки на кросс-валидации дополнительный feature engineering не был включен в финальную модель, так как он не улучшил качество.

Вероятная причина — небольшой размер обучающей выборки и высокая коррелированность новых признаков с уже существующими RDKit-дескрипторами.

В финальном решении используется следующий подход:

* исходные молекулярные признаки;
* заполнение пропусков медианой внутри `Pipeline`;
* удаление константных признаков через `VarianceThreshold`;
* при необходимости отбор признаков через `SelectKBest`.
---

## Модель 1: таргет-специфичный ансамбль деревьев

На базовом этапе сравнивались несколько моделей для табличных данных:

* `ExtraTreesRegressor`;
* `RandomForestRegressor`;
* `HistGradientBoostingRegressor`.

Финальное решение использует отдельные модели для каждого таргета. Такой подход выбран потому, что `IC50`, `CC50` и `SI` имеют разные масштабы, разные распределения и по-разному реагируют на преобразование таргета.

Для подбора параметров используется сетка по:

* типу ансамбля;
* `min_samples_leaf` как параметру регуляризации деревьев;
* `max_features` как способу контролировать разнообразие деревьев;
* количеству признаков `k` в `SelectKBest`;
* весу сырой компоненты `w_raw` для `IC50` и `CC50`.

Для `IC50` и `CC50` дополнительно используется смесь двух режимов обучения:

* модель на `log1p(target)`;
* модель на исходном target.

Это позволяет совместить устойчивость log-модели на основной массе объектов и способность raw-модели лучше учитывать большие значения.

Для `SI` дополнительно проверялся `HistGradientBoostingRegressor`, так как индекс селективности имеет сложное распределение и сильные выбросы. Однако в финальной схеме с `log1p`-таргетом, отбором признаков и seed-ансамблем `RandomForestRegressor` показал более устойчивый результат, поэтому для `SI` был оставлен Random Forest.

Результат первой модели сохраняется в файл:

```text
submission_1.csv
```

Public Kaggle score для первой модели:

```text
293.8 RMSE
```
---

## Модель 2: независимая QSAR-SVR модель

После анализа предметной области была добавлена вторая независимая QSAR-модель.

QSAR (*Quantitative Structure–Activity Relationship*) — это подход, в котором биологическая активность молекулы предсказывается по численным молекулярным дескрипторам. В данном соревновании исходные признаки уже представлены как набор молекулярных дескрипторов, поэтому была протестирована дополнительная модель:

```text
SimpleImputer - StandardScaler - SelectKBest - SVR
```

Модель обучается отдельно для `IC50`, `CC50` и `SI` на `log1p(target)`.

Результат второй модели сохраняется в файл:

```text
submission_2.csv
```

Public Kaggle score для второй модели:

```text
308.12 RMSE
```

Отдельно эта модель оказалась слабее основной, но ее предсказания отличаются от предсказаний первого подхода. Поэтому она была использована как независимая компонента финального усреднения.

---

## Валидация

Качество оценивалось с помощью кросс-валидации на обучающей выборке. Используется метрика, соответствующая логике Kaggle:

```text
score = mean(RMSE(IC50), RMSE(CC50), RMSE(SI))
```

Дополнительно анализировалось качество на центральной части train-распределения по квантилям таргетов. Это нужно из-за того, что полный RMSE на небольшой выборке сильно зависит от редких экстремальных значений.

Такая проверка используется только для анализа устойчивости модели и не использует тестовые ответы.

---

## Финальное решение

Финальное решение состоит из двух независимых подходов:

1. `submission_1.csv` — таргет-специфичный ансамбль деревьев;
2. `submission_2.csv` — независимая QSAR-SVR модель.

Итоговый файл `submission.csv` получается простым равновесным усреднением:

```text
submission = 0.5 * submission_1 + 0.5 * submission_2
```

Такое усреднение снижает дисперсию предсказаний и частично компенсирует ошибки отдельных моделей. Вес 50/50 выбран как простой и воспроизводимый вариант без агрессивной подгонки под leaderboard.

Результаты на public leaderboard:

* `submission_1.csv` — `293.8 RMSE`;
* `submission_2.csv` — `308.12 RMSE`;
* финальный `submission.csv` — `279.50 RMSE`.

Все random seed зафиксированы в ноутбуке, чтобы результат можно было воспроизвести.

---

## Инструкция по воспроизведению результата

### 1. Клонировать репозиторий

```bash
git clone https://github.com/data-sp/dpo-hakaton-peresdacha
cd dpo-hakaton-peresdacha
```

### 2. Подготовить окружение

Для запуска ноутбука локально потребуется Python 3 и следующие пакеты:

- `numpy`
- `pandas`
- `matplotlib`
- `scikit-learn`
- `jupyter`

Их можно установить командой:

```bash
pip install numpy pandas matplotlib scikit-learn jupyter
```

### 3. Скачать данные

Данные не хранятся в репозитории. Их нужно скачать по ссылке, предоставленной организаторами хакатона.

После скачивания положить файлы в корень проекта рядом с ноутбуком:

train.csv
test.csv
sample_submission.csv

Ноутбук ожидает следующие имена файлов:

```python
train = pd.read_csv("train.csv")
test = pd.read_csv("test.csv")
sample_submission = pd.read_csv("sample_submission.csv")
```

### 4. Запустить ноутбук

```bash
jupyter notebook ChemAI_final.ipynb
```

Далее в Jupyter Notebook выполнить:

```text
Kernel → Restart & Run All
```

После выполнения всех ячеек будут созданы файлы:

```text
submission_1.csv
submission_2.csv
submission.csv
```

Файл `submission.csv` является итоговым файлом для отправки на Kaggle.
---

## Итог

В результате был построен воспроизводимый пайплайн для предсказания трех показателей активности химических соединений. Решение сочетает таргет-специфичный ансамбль деревьев и независимую QSAR-SVR модель. Пропуски обрабатываются внутри пайплайна, параметры подбираются на кросс-валидации, а финальный `submission.csv` формируется простым усреднением двух подходов.

**Финальный public Kaggle score:** `279.50 RMSE`


